import React from 'react'
import Template from "@/Components/Template";

const Home = () => {
    let f: string = 'React'
    const bar: string = 'TypeScript'
    return (
        <Template/>
    )
}

export default Home
